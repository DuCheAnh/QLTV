<UserControl x:Class="TestGUI_QLTV.MainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestGUI_QLTV"
             FontFamily="Comic Sans MS">
    <Grid Height="630" Width="1280">
        <Grid.RowDefinitions>
            <RowDefinition Height="600"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel x:Name="bookList" Grid.Row="0" Width="1260" Height="600">
            <ItemsControl x:Name="IBook" ItemsSource="{Binding Products}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="280" Height="130" Background="White" Margin="15 10">
                            <Button Height="auto" Width="auto" Background="White" BorderBrush="Black" BorderThickness="1" Click="Button_Click" >
                                <Canvas Height="120" Width="260">
                                    <StackPanel Height="120" Width="90" Canvas.Left="0" Canvas.Top="0">
                                        <StackPanel.Background>
                                            <ImageBrush ImageSource="{Binding bImage}"></ImageBrush>
                                        </StackPanel.Background>
                                    </StackPanel>
                                    <Label Name="lbBookName" Content="{Binding bName}" Canvas.Left="95" FontSize="16"/>
                                    <Label Content="{Binding bActor}" Canvas.Left="95" Canvas.Top="30"/>
                                </Canvas>
                            </Button>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <!--<Grid Width="1280" Height="600">
                <StackPanel x:Name="dpBook"></StackPanel>
            </Grid> -->
        </StackPanel>
        <Button x:Name="btnNextPage" Grid.Row="1" Click="btnNextPage_Click"></Button>
       
    </Grid>
</UserControl>
